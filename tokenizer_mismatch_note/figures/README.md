# Figures

- `fig1_inflation_vs_divergence.pdf` — token inflation vs. normalized byte-n-gram
  divergence, one point per corpus, with both through-origin fits (all six corpora:
  slope 1.38, R² = 0.47; excluding the Hebrew coverage outlier: slope 0.741,
  R² = 0.990). Copied from `../../tokshift/figures/` (source of truth; regenerate
  there if the experiment is re-run).
